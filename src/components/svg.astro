---
interface Props {
  size: number;
  path: string;
  class?: string;
}

const { size, path, class: className } = Astro.props;
---

<svg
  class={className}
  xmlns="http://www.w3.org/2000/svg"
  viewBox={`0 0 ${size} ${size}`}
>
  <path d={path}></path>
</svg>
